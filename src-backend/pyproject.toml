[project]
name = "src-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sanic>=25.3.0",
    "sqlalchemy>=2.0.41",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.14.1",
]
